{
  "name": "product-management-system",
  "version": "1.0.0",
  "description": "A full-stack Go application for managing products with both CLI and web interfaces",
  "scripts": {
    "build-css": "tailwindcss -i ./src/input.css -o ./static/styles.css",
    "watch-css": "tailwindcss -i ./src/input.css -o ./static/styles.css --watch",
    "dev": "concurrently \"npm run watch-css\" \"go run . web\"",
    "build": "npm run build-css && go build -o product-manager ."
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^9.2.1",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.16"
  }
}
